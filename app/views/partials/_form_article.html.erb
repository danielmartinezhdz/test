<%= form_for(@article,remote: true) do |t| %>
	<div>
		<%= t.text_field :title, placeholder: "Title"%>
	</div>
	<div>
		<%= t.text_area :body, placeholder: "Cuerpo" %>
	</div>
	<div>
		<%= t.submit "Guardar", class: "btn btn-success"%>
	</div>
<%end%>